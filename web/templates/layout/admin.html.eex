<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>CEDA Realty Admin Panel</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="CEDA Realty, reinventing the real estate experience!">
    <meta name="author" content="CEDA Realty">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>
  <body>
    <div class="container">
      <p class="alert alert-info"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger"><%= get_flash(@conn, :error) %></p>
      <nav id="login-nav" class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="login-nav-navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= if @conn.assigns[:current_user] do %>
              <span class="navbar-brand">Hello, <%= @conn.assigns[:current_user].username %></span>
            <% end %>
          </div>
          <div id="login-nav-navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <%= if @conn.assigns[:current_user] do %>
                <li><%= link "Log Out", to: session_path(@conn, :delete), method: :delete %></li>
              <% else %>
                <li><%= link "Log In", to: session_path(@conn, :new) %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
      <%= @inner %>
    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script>require("web/static/js/app")</script>
  </body>
</html>
